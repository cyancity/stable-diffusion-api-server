name: sd-api-server
channels:
  - pytorch
  - nvidia
dependencies:
  - python=3.10
  - pip
  - cudatoolkit=11.7
  - pytorch-cuda
  - pytorch
  - torchvision
  - numpy
  - pip:
      - requests
      - diffusers
      - transformers
      - Pillow
      - Flask
      - accelerate
      - ftfy
      - requests-toolbelt